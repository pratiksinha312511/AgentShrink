# ============================================================
# AgentShrink — requirements.txt
# Pinned versions to avoid LangChain API drift
# Tested on 8GB RAM machine
# ============================================================

# --- Core LangChain stack ---
langchain==0.3.7
langchain-core==0.3.19
langchain-openai==0.2.8
langchain-ollama==0.2.0
langchain-google-genai==2.0.9
langgraph==0.2.45

# --- Local model inference ---
ollama==0.3.3               # Python client for Ollama server

# --- Embedding + clustering (Phase 2) ---
sentence-transformers==3.2.1
hdbscan==0.8.41
umap-learn==0.5.6
scikit-learn==1.6.1

# --- Data handling ---
pandas==2.2.3
numpy==1.26.4

# --- NLP + PII removal (Phase 2) ---
spacy==3.7.5                # run: python -m spacy download en_core_web_sm

# --- Dashboard (Phase 5) ---
streamlit==1.39.0
plotly==5.24.1
fastapi==0.115.5
uvicorn==0.32.0

# --- CLI ---
click==8.1.7
openai==1.54.4
huggingface_hub==0.26.2

# --- Fine-tuning (Phase 6 — Colab only, not local) ---
# huggingface-hub==0.24.6   # uncomment when doing fine-tuning on Colab
# datasets==2.21.0          # uncomment when doing fine-tuning on Colab

# --- Utilities ---
python-dotenv==1.0.1        # load OPENAI_API_KEY from .env file
rich==13.8.1                # beautiful terminal output
tqdm==4.66.5               # progress bars

# ============================================================
# INSTALL ORDER (important on 8GB machine):
# 1. pip install -r requirements.txt
# 2. python -m spacy download en_core_web_sm
# 3. Install Ollama separately from ollama.com
# 4. ollama pull gemma2:2b
# 5. ollama pull llama3.2:3b
# 6. ollama pull phi3.5:mini    (do this LAST — largest file)
# ============================================================
